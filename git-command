🔹 Basic

Q1. What is Git?
➡ Distributed version control system to track code changes.

Q2. Git vs GitHub?
➡ Git = tool, GitHub = hosting platform.

Q3. What is a repository?
➡ A place where Git stores project history.

🔹 Intermediate

Q4. Difference between git fetch and git pull?
    fetch → downloads only
    pull → fetch + merge

Q5. What is HEAD?
➡ Pointer to the current commit.

Q6. What is staging area?
➡ Intermediate area before commit.

🔹 Advanced

Q7. What is rebase?
➡ Rewrites commit history to keep it linear.

Q8. What is cherry-pick?
➡ Apply a specific commit from another branch.

Q9. How to undo a commit?
git reset --soft HEAD~1
git reset --hard HEAD~1
git revert <commit-id>

✅ 2️⃣ Git Rebase vs Merge (VERY IMPORTANT)
🔹 Conceptual Difference
| Feature               | Rebase        | Merge       |
| --------------------- | ------------- | ----------- |
| History               | Linear        | Branching   |
| Commits               | Rewritten     | Preserved   |
| Safe on shared branch | ❌             | ✅           |
| Used for              | Clean history | Team safety |

🔹 Merge Example
git checkout main
git merge feature
--> 📌 Creates a merge commit

🔹 Rebase Example
git checkout feature
git rebase main
--> 
📌 Moves feature commits on top of main

✅ 3️⃣ Git Workflow for Teams (Industry Standard)
---------------------------------------------------
🔹 Git Flow (Most Used) : 
main       → production
develop    → integration
feature/*  → new features
release/*  → pre-production
hotfix/*   → urgent prod fixes

🔹 Feature Workflow
git checkout develop
git checkout -b feature/login

git add .
git commit -m "Add login feature"

git push origin feature/login

--> Create Pull Request

🔹 Pull Request Flow
Feature branch
   ↓
Pull Request
   ↓
Code Review
   ↓
Merge to develop/main
========================================================================================================================================

✅ Following Steps to upload the project into github : 
==========================================================

1️⃣ Initialized Git 
git init   : : git init initializes an empty Git repository by creating the .git metadata directory.

✔ Repository created correctly

2️⃣ Staged all files
git add .   : All files moved from working directory → staging area

Note: After `git add .` , you will show CRLF / LF Warnings
Meaning:
  1.You are on Windows
  2.Git is warning about line-ending conversion

| OS          | Line Ending |
| ----------- | ----------- |
| Windows     | CRLF        |
| Linux/macOS | LF          |


✔ All project files added
⚠ LF → CRLF warnings → harmless on Windows

3️⃣ Verified staging
git status


✔ Files correctly staged
✔ No mistakes

4️⃣ First commit
git commit -m "first commit"

Q: What happened internally?
--> Git created:
            1. A commit object
            2. A tree object
            3. Multiple blob objects

--> The commit now has:
            1. Commit ID: aee1af7
            2. Author
            3. Timestamp
            4. Snapshot of all staged files
✔ Clean root commit
✔ 15 files committed

5️⃣ Set main branch
git branch -M main

Q: What happened?
--> Branch renamed to main
--> -M = force rename

Q: Why this matters:
--> GitHub uses main as default branch


6️⃣ Connected to GitHub
git remote add origin https://github.com/Sahniajaykumar/card-components.git

Q: What happened?
--> Git stored a remote reference
--> Name: origin
--> URL: GitHub repository

✔ Remote added correctly

7️⃣ Pushed to GitHub
git push -u origin main


Q: What happened?
--> Git packaged commits
--> Sent them to GitHub
--> Created branch main on remote
--> Uploaded all files

📌 -u means: 
--> Set upstream
--> Future git push works without arguments

✔ Push successful
✔ Tracking set up
✔ Repo is live 🚀
=========================================================================================================================================

After deploye project if you want to update some feature in your deployed project then again follow this procedure

2️⃣ Staged all files
git add .

4️⃣ First commit
git commit -m "first commit"                      // anything message

7️⃣ Pushed to GitHub

git push -u origin main

✔ Push successful
✔ Tracking set up
✔ Repo is live 🚀
========================================================================================================================================
GitHub repository already has commits : 
----------------------------------------

Step 1: Pull remote changes first
git pull origin main --allow-unrelated-histories

--> 📌 --allow-unrelated-histories is needed because:
--> Local repo and remote repo were created separately

Step 2: Resolve merge (if asked)
git add .
git commit -m "Merge remote and local changes"

Step 3: Push again
git push -u origin main
=========================================================================================================================================

Netlify : Production ready deploy

1. npm run build
--> one brand new folder(dist) will created , just drag and drop it to netlify
